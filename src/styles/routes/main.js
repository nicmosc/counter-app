import { StyleSheet } from 'react-native';

import vars from '../vars';


export default StyleSheet.create({
  main: {
  },
  items: {
    marginVertical: 10,
    marginHorizontal: 50,
    display: 'flex',
  },
});
