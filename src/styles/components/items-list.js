import { StyleSheet } from 'react-native';

import vars from '../vars';


export default StyleSheet.create({
  itemsList: {
    backgroundColor: 'red',
    paddingTop: 30,
    paddingBottom: 30,
  },
});
